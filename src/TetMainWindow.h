////////////////////////////////////////////////////////////////////////////////////////////

#ifndef __TETMAINWINDOW_H__
#define __TETMAINWINDOW_H__

////////////////////////////////////////////////////////////////////////////////////////////

#define PDRAWBUF_SIZE	1024

////////////////////////////////////////////////////////////////////////////////////////////

class GTetMsgLoop : public GMsgLoop
{
public:
	void Idle();
};

////////////////////////////////////////////////////////////////////////////////////////////

class GTetMainWindow : public GWindow, GWindow::GEvents
{
public:
	GTetMainWindow();
	bool OnCreate(GWindow *pWindow);
	void OnDestroy(GWindow *pWindow);
	LRESULT OnCommand(GWindow *pWindow, HWND hwndFrom, UINT idFrom, UINT code);
	virtual LRESULT WndProc(HWND hWnd, UINT uMsg, WPARAM wParam, LPARAM lParam);

private:	
	COLORREF *m_pDrawBuf;
	GMenu m_mainMenu;
};

////////////////////////////////////////////////////////////////////////////////////////////

#endif

////////////////////////////////////////////////////////////////////////////////////////////
