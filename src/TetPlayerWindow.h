////////////////////////////////////////////////////////////////////////////////////////////

#ifndef __TETPLAYERWINDOW_H__
#define __TETPLAYERWINDOW_H__

////////////////////////////////////////////////////////////////////////////////////////////

class GTetPlayerWindow : public GWindow, GWindow::GEvents
{
public:
	GTetPlayerWindow();

	bool Create(GTetPlayer *pPlayer);
	bool OnCreate(GWindow *pWindow);
	void OnDestroy(GWindow *pWindow);
	LRESULT WndProc(HWND hWnd, UINT uMsg, WPARAM wParam, LPARAM lParam);

	bool getdock() {return m_bDockWindow;};
	void setdock(bool bDock);

private:
	GTetPlayer *m_pPlayer;
	HBITMAP m_hBackgroundBitmap;
	bool m_bDockWindow;
};

////////////////////////////////////////////////////////////////////////////////////////////

#endif

////////////////////////////////////////////////////////////////////////////////////////////
